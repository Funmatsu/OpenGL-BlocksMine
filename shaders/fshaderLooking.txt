#version 330 core
                                       
out vec4 color;
uniform sampler2D theTexture;
in vec4 pdc;
in vec2 texCoords;
in float transparency;

void main(){
    color = texture(theTexture, texCoords) * vec4(0.0f, 0.0f, 0.0f, 0.2f);
};
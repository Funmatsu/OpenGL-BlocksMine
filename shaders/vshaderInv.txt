#version 330 core                                                   
                                                                    
layout (location = 0) in vec3 pos;
layout (location = 1) in vec2 inTex;

out vec2 texCoords;
                                                                    
uniform mat4 ortho;       
                                                                    
void main(){                                                        
    	gl_Position = ortho * vec4(pos, 1.0);
	
	texCoords = inTex;
};